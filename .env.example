# Elastic Cloud / Serverless credentials
ELASTICSEARCH_URL=https://your-deploy.es.us-east-1.aws.elastic.cloud:443
ELASTIC_API_KEY=your-api-key-here

# Optional: set explicitly if auto-derivation from ES URL doesn't work
# KIBANA_URL=https://your-deploy.kb.us-east-1.aws.elastic.cloud:443
